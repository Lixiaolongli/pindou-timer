# 拼豆计时器 功能清单

> 拼豆创意手工坊 · 计时管理系统  
> 技术栈：Cloudflare Pages + Cloudflare KV + 纯 HTML/CSS/JS  
> 线上地址：https://pindou-timer.pages.dev

---

## 一、系统架构

| 角色 | 说明 |
|------|------|
| 👑 管理员 | 管理所有商家账号，增删改查、续费/减天数 |
| 🏪 商家 | 每个商家一个店铺，管理订单、套餐、密码 |
| 👤 顾客 | 扫码输入手机尾号查看自己的计时状态 |

---

## 二、管理员功能

### 账号管理
- [x] 手机号 + 密码登录
- [x] 修改自己的密码
- [x] 添加商家账号（手机号 + 名称 + 密码 + 到期天数，默认30天）
- [x] 按角色显示中文标签（管理员 / 商家）
- [x] 删除商家（连带删除店铺、订单、清理 token 强制下线）
- [x] 禁用商家（清理 token 强制下线）

### 天数管理
- [x] 增加商家有效期天数（快捷选 7/15/30/90/180/365 天 + 自定义）
- [x] 减少商家有效期天数（二次确认弹窗）
- [x] 到期时间至少保留 1 天
- [x] 续费记录日志（增/减天数、操作人、时间，管理员可见）

### 自动化
- [x] 每日 8:00 cron 自动检查并禁用到期商家
- [x] 登录时实时检查到期状态

---

## 三、商家功能

### 订单管理
- [x] 创建订单（手机尾号 + 单人/双人 + 时长 + 价格）
- [x] 套餐快捷选择（一键填充时长和价格）
- [x] 前台倒计时每秒实时更新
- [x] 单个订单完成
- [x] 批量完成订单（勾选 + 批量按钮）
- [x] 订单续时（增加时长 + 追加价格）
- [x] 订单按剩余时间自动排序（到期优先 → 不限时最后）
- [x] 点击订单卡片展开操作按钮（完成/续时）

### 搜索与筛选
- [x] 搜索手机尾号
- [x] 状态下拉筛选（全部 / 进行中 / 已完成）

### 套餐管理
- [x] 添加自定义套餐（名称 + 单人/双人 + 时长 + 价格）
- [x] 删除套餐
- [x] 一键重置为默认套餐
- [x] 每个商家套餐独立存储

### 数据导出
- [x] 导出 CSV（订单号、类型、价格、时长、状态、开始时间、结束时间）

### 账户设置
- [x] 修改店铺名称
- [x] 修改登录密码
- [x] 查看 VIP 天数（开通时间、到期时间、剩余天数）
- [x] 查看操作日志（下单/完成/续时记录）

### 营收统计
- [x] 今日营收金额
- [x] 今日订单总数
- [x] 进行中数量
- [x] 已完成数量
- [x] 每 5 秒自动刷新数据

---

## 四、顾客端

- [x] 扫码进入（二维码自动生成）
- [x] 输入手机后 4 位查看订单计时
- [x] 倒计时实时每秒更新
- [x] 时间到时显示"请到吧台结账或续时"
- [x] 查看历史订单记录（同手机尾号）

---

## 五、提醒与通知

- [x] 订单到期播放 beep 提示音（Web Audio API，无需音频文件）
- [x] 商家面板进行中订单自动刷新（5 秒同步后端，前端 1 秒刷新 UI）

---

## 六、UI/UX

- [x] 拼豆主题装饰（漂浮豆子 + 3×3 拼豆 Logo + 底板网格）
- [x] 暖色调渐变背景 + 奶油色卡片
- [x] 底部固定 Tab 导航（订单 / 新订单 / 套餐 / 入口 / 更多）
- [x] ⚙️ 更多弹出菜单
- [x] Toast 消息提示
- [x] 响应式布局，适配手机端
- [x] 安全区域适配（iPhone 底部 bar）

---

## 七、安全机制

- [x] 密码 SHA-256 哈希存储
- [x] Token 认证（24 小时有效期）
- [x] 管理员接口权限校验（role === 'admin'）
- [x] 商家接口权限校验（role === 'merchant'）
- [x] 删除/禁用商家时清除 token 强制下线
- [x] 到期商家自动禁止登录

---

## 八、后端 API 列表

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/login` | 登录 |
| POST | `/api/change-password` | 修改密码 |
| GET | `/api/merchants` | 管理员获取商家列表 |
| POST | `/api/merchants` | 管理员操作（add/delete/disable/renew） |
| GET | `/api/renew-logs` | 续费记录 |
| GET | `/api/shops` | 商家获取自己的店铺 |
| POST | `/api/shops` | 创建店铺 / 修改店名 |
| GET | `/api/shops/:id` | 顾客获取店铺信息 |
| GET | `/api/packages` | 获取套餐 |
| POST | `/api/packages` | 管理套餐（add/delete/reset） |
| GET | `/api/orders` | 获取订单列表（支持 search + status 筛选） |
| POST | `/api/orders` | 创建订单 |
| POST | `/api/orders/complete` | 完成单个订单 |
| POST | `/api/orders/batch-complete` | 批量完成订单 |
| POST | `/api/orders/extend` | 续时 |
| POST | `/api/orders/remove` | 删除订单 |
| GET | `/api/orders/lookup` | 顾客查订单（公开） |
| GET | `/api/orders/history` | 顾客订单历史 |
| GET | `/api/logs` | 操作日志 |
| GET | `/api/stats` | 今日营收统计 |
